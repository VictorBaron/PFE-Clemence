{{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="Valider" />
    </div>
{{ form_end(form) }}

{{ offre.id }}
{{ offre.lender.username }}
{{ offre.project.id }}