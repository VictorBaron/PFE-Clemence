{% extends 'base.html.twig' %}

{% block title %} Clemence - {{ name }} {% endblock %}

{% block contenu %}

{% if contrat %}
  Ce projet est associé à un contrat de prêt signé ! Vous ne pouvez pas le supprimer.
{% else %}
  <h2>Supprimer un projet</h2>

  <p>
    Etes-vous certain de vouloir supprimer le projet "{{ project.title }}" ?
    <br> Cela ne prendra effet que si vous n'avez aucun contrat associé.
  </p>

  {# On met l'id de l'annonce dans la route de l'action du formulaire #}
  <form action="{{ path('delete_project', {'id': project.id}) }}" method="post">
    <a href="{{ path('view_project', {'id': project.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour au projet.
    </a>
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
  </form>
{% endif %}
{% endblock %}